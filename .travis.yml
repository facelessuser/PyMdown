language: python
env:
  - TOXENV=py27-unittests
  - TOXENV=py33-unittests
  - TOXENV=py34-unittests
  - TOXENV=lint
install:
  - pip install tox
  - pip install coveralls
script:
  - tox
after_success:
  - coveralls
