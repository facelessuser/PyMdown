{
    // By default, mdown accepts proposed critic markups,
    // you can change this behaviour to reject the markups
    "reject": false,

    // Point to the mdown binary
    "binary": {
        "windows": "mdown.exe",
        "osx": "mdown",
        "linux": "mdown"
    }
}
